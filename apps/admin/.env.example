# Real backend, server-side only — the browser never talks to it directly
# (no CORS headers on the upstream), it goes through this app's own same-origin
# proxy at /api/proxy. Defaults to the Railway deployment if unset.
GWANI_API_BASE_URL=
